[project]
name = "pdf-search"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = ["flask>=3.1.3"]

[dependency-groups]
dev = [
    "flake8>=7.3.0",
    "isort>=8.0.1",
    "ruff>=0.15.18",
    "ty>=0.0.51",
]
